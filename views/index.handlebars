<h1>Burgers!</h1>
<h2>Burgers are tasty!</h2>
<img src="/public/assets/imgs/burger_logo.jpg">

<ul>

  {{#each allBurgers}}
    {{#unless this.devoured}}
      <div>
        <button class='addButton' value={{id}}>{{burger_name}}</button>
        {{!-- <form class="change-devour">
          <input class="devoured" type="hidden" value={{id}}>
          <button type="submit" class="addbutton"> {{burger_name}}<button>
        </form> --}}
      </div>
    {{/unless}}
  {{/each}}
</ul>

<ul>
  {{#each allBurgers}}
    {{#if devoured}}
      <div class='burgerEaten'>
          <div>
            {{this.burger_name}}
          </div>
      </div>
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Burger</h2>
{{!-- <form class="create-form">

  <div class="form-group">
    <label for="ca">Burger Name:</label>
    <input type="text" id="ca" name="name">
  </div>

  <button type="submit">Add Burger</button>
</form> --}}

<form action="/burgers" method="POST">
  <input type="text" id="name" name="burger_name" placeholder="first burger">
  <button type="submit" class="addburger">Add The Burger</button>
</form>